<% labels = [:name, :date] %>

<% form_for event do |f| %>
  <% labels.each do |lab| %>
    <p>
      <%= f.label lab %>
      <%= f.text_field lab %>
    </p>
  <% end %>
  <p>
    <%= f.submit %>
  </p>
<% end %>
